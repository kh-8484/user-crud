// router.post("/signIn", (req, res) => {
//   try {
//     const { name, mobile, password, email } = req.body;

//     if (!name || !mobile || !password || !email) {
//       let err = {
//         status: 400,
//       };
//     }
//   } catch (error) {}
// });
